import {
    Container,
} from "./styledCartProduct";

export const CartProduct = ({ item }) => {
    return (
        <Container>
            <h1>CartProduct Component</h1>
        </Container>
    );
};
